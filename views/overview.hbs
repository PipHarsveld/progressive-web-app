<header>
    {{> header}}
    {{> headerOverview}}
</header>
<main class="overview">
    <h2>Uitgelicht</h2>
    <ul>
        {{#each data}}
        <li>
            <a href="/details/{{this.id}}">
                <img src="{{this.webImage.url}}" alt="{{this.title}}">
            </a>
        </li>
        {{/each}}
    </ul>
</main>